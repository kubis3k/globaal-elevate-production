---
interface Props {
  as?: 'h2' | 'h3';
}

const { as = 'h2' } = Astro.props;
const Tag = as;
---

<Tag
  class:list={[
    'font-bold text-ink',
    as === 'h2' ? 'text-2xl sm:text-3xl' : 'text-xl sm:text-2xl',
  ]}
>
  <slot />
</Tag>
